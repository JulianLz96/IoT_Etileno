import { LoggeadoDirective } from './loggeado.directive';

describe('LoggeadoDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggeadoDirective();
    expect(directive).toBeTruthy();
  });
});
